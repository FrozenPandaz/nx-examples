/*
 * This Java source file was generated by the Gradle 'init' task.
 */
package created.gradle.app.app;

import created.gradle.app.list.LinkedList;

import static created.gradle.app.utilities.StringUtils.join;
import static created.gradle.app.utilities.StringUtils.split;
import static created.gradle.app.app.MessageUtils.getMessage;

import org.apache.commons.text.WordUtils;

public class App {
    public static void main(String[] args) {
        LinkedList tokens;
        tokens = split(getMessage());
        String result = join(tokens);
        System.out.println(WordUtils.capitalize(result));
    }
}
